server:
  port: 8002

spring:
  profiles:
    active: prod
  application:
    name: serivce-oss
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

aliyun:
  oss:
    file:
      endpoint: oss-cn-beijing.aliyuncs.com   # 节点
      keyid: XXX  # 秘钥id
      keysecret: XXX  # 秘钥
      bucketname: edu-wangs  # bucket名称
